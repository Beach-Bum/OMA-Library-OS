__pycache__/
*.pyc
*.faid
/tmp/


# Added by cargo

/target
/target/
Cargo.lock
# _content-backup/ is the MDX source build.py compiles into site/.
# It is intentionally tracked so the site is reproducible from the repo.
.gstack/
